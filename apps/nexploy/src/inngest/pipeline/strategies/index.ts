// Base
export { BaseStrategy } from './base.strategy';

// Strategy implementations
export { DockerfileStrategy, dockerfileStrategy } from './dockerfile.strategy';
export { DockerComposeStrategy, dockerComposeStrategy } from './compose.strategy';

// Future strategies can be added here:
// export { NixpacksStrategy, nixpacksStrategy } from './nixpacks.strategy';
// export { BuildpacksStrategy, buildpacksStrategy } from './buildpacks.strategy';
