model DeploymentStage {
  id           String  @id @default(cuid())
  name         String
  isProduction Boolean @default(false)

  repositoryId String
  repository   Repository @relation(fields: [repositoryId], references: [id], onDelete: Cascade)

  environmentId String?
  environment   Environment? @relation("StageEnvironment", fields: [environmentId], references: [id], onDelete: SetNull)

  requiredStageId String?
  requiredStage   DeploymentStage?  @relation("StageProtection", fields: [requiredStageId], references: [id], onDelete: SetNull)
  protectedStages DeploymentStage[] @relation("StageProtection")

  envVariables EnvVariable[]
  versions     Version[]
  builds       Build[]
  pipelineConfig PipelineConfig?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([repositoryId])
  @@index([requiredStageId])
  @@map("deployment_stage")
}
