model BackupSchedule {
  id              String        @id @default(cuid())
  volumeName      String
  environmentId   String?
  bucket          String
  bucketStorageAccountId     String
  bucketStorageAccount       BucketStorageCredential  @relation(fields: [bucketStorageAccountId], references: [id], onDelete: Cascade)
  frequency       Frequency
  scheduledHour   Int           @default(0)
  scheduledMinute Int           @default(0)
  scheduledDay    Int?
  lastRunAt       DateTime?
  nextRunAt       DateTime
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt

  @@map("backup_schedule")
}

enum Frequency {
  HOURLY
  DAILY
  WEEKLY
  MONTHLY
}
