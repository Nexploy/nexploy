model BucketStorageCredential {
  id              String           @id @default(cuid())
  displayName     String
  accessKeyId     String
  secretAccessKey String
  region          String
  endpoint        String?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  schedules       BackupSchedule[]

  @@map("bucket_storage_credential")
}
